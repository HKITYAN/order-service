sudo: required
services:
  - docker
  - mysql
dist: xenial

before_install:
  - docker-compose up -d
  - service mysql status
  - docker exec -i mysql-service mysql -u root -psecretpassword

script:
  - echo hi
